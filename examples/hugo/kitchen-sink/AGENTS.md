# Hugo Kitchen-Sink

Hugo Extended + Tailwind CSS 4 static site with TinaCMS admin-panel editing, block-based pages, rich-text content, and multi-collection schemas. Ships as a fully static site with zero client-side JavaScript in production.

## Architecture

Two-layer model:

1. **Hugo templates** (`layouts/`) — file-based routing, data fetching via Hugo's `site.GetPage`, static generation.
2. **TinaCMS admin panel** (`/admin/`) — React-based editing UI, auto-generated by TinaCMS at build/dev time.

Hugo cannot use `useTina()`; all content editing happens via the admin panel. Visual/inline editing requires React, which the rendered site does not ship.

## Coding Standards

Repo-wide Biome + strict TypeScript apply (see root [AGENTS.md](../../../AGENTS.md)). Shared TinaCMS patterns (admin UI selectors, slugify, GraphQL quirks, E2E conventions) are documented in [examples/AGENTS.md](../../AGENTS.md). App-specific deltas:

- Format JS/TS/JSON with `npx @biomejs/biome format --write <file>` after edits.
- Use `cascade.target` in Hugo cascade config — `cascade._target` was deprecated in Hugo 0.156.0.
- Use `hugo.Data` rather than `site.Data` in templates — the latter was deprecated in Hugo v0.156.0.

Collection schemas live in [tina/collections/](tina/collections/).

## Common Commands

- `pnpm dev` — `tinacms dev -c "hugo-extended server"` (TinaCMS wraps Hugo)
- `pnpm build` — `tinacms build && hugo-extended`
- `pnpm build:local` — `tinacms build --local --skip-cloud-checks -c "hugo-extended"`
- `pnpm serve` / `pnpm build:hugo` — Hugo-only, skip TinaCMS
- `pnpm test:e2e` / `pnpm test:e2e:ui` — Playwright

## Key Patterns

### Admin-only editing

Content is edited via the TinaCMS admin UI at `/admin/`. No inline/visual editing — that requires React `useTina()`, which this app does not ship.

### Hugo module mounts

Hugo accesses shared content via `[[module.mounts]]` in `hugo.toml` (e.g. `../../shared/content/authors` → `content/authors`). Do NOT use directory symlinks for content — Hugo's Go-based file walker does not follow them on Windows. Module mounts are Hugo's official cross-platform mechanism. When any mount is specified, defaults are overridden — re-declare `layouts`, `static`, `assets`, `archetypes`.

### Hugo static folder

Hugo uses `static/` (not `public/`). TinaCMS config sets `build.publicFolder: 'static'` and `media.tina.publicFolder: 'static'`.

### Image asset symlinks

`static/uploads` and `static/blocks` are symlinked to the shared public directory for shared assets. Static symlinks work fine cross-platform — Hugo copies these differently than content.

### Data-only collections

Tag and Global collections use `build: {render: never, list: never}` in Hugo cascade config to prevent page generation. Tags are accessed via `site.GetPage` for reference resolution. Global config is accessed via `hugo.Data.global.index` (mounted into Hugo's data directory as well as content).

### Global config dual mount

The shared global directory is mounted as BOTH `content/global` (for TinaCMS) and `data/global` (for Hugo templates). Hugo's `readFile`/`fileExists` can't reach files outside the project root, but `hugo.Data` reads from data mounts natively.

### Content format

All content uses `.md` format. Rich-text body is rendered via Hugo's Goldmark markdown renderer — the shared `TinaMarkdown` custom components (BlockQuote, DateTime, NewsletterSignup) are NOT available here.

### Non-body rich-text fields

Fields like `excerpt` and hero `text` use `string` type with `textarea` UI instead of `rich-text`, since Hugo cannot render rich-text AST stored in frontmatter.

### Reference resolution

TinaCMS stores references as path strings (e.g. `content/authors/napoleon.md`). Hugo templates resolve these using `site.GetPage` with `path.BaseName` to extract the slug. Inside `{{ with }}` blocks, use `$.Site.GetPage` or the global `site.GetPage` — the context `.` changes to the field value.

### Dynamic HTML tags

Hugo's auto-escaping converts `<{{ $tag }}>` to `&lt;h1>`. Use `printf` + `| safeHTML` for partials with dynamic tag names (e.g. `gradient-title`).

### Block rendering

Page collection uses block-based content. Hugo templates dispatch to block partials via `{{ partial (printf "blocks/%s.html" ._template) . }}`. 5 block types: `hero`, `features`, `cta`, `testimonial`, `content`.

### Image path security

All `<img>` tags with CMS-controlled URLs use inline `hasPrefix` checks — only allow relative paths (`/`, `./`, `../`) and `http://`/`https://` URLs. This is Hugo's equivalent of the `sanitizeImageSrc()` utility used by the React-ish apps.

### Dark-mode detection

An inline `<script>` in `<head>` (before page render) checks `window.matchMedia('(prefers-color-scheme: dark)')` and adds the `dark` class to `<html>`. Must be inline (not deferred) to prevent a flash of wrong theme.

### Template variable scoping

Inside `{{ range }}`, `$` refers to the template root (the listing page), NOT the current item. Capture page fields into variables (e.g. `{{ $postDate := .Params.date }}`) before entering nested `{{ with }}` blocks to avoid reading from the wrong context.

### Card link styling

Post and author listing cards use the `cardLinkClasses` pattern — gradient background (`from-gray-50 to-gray-100 dark:from-gray-900 dark:to-gray-1000`) with rounded corners and shadow hover effect.

## Partial Architecture

**Section wrappers:**
- `section.html` — Color-variant wrapper (`default`/`tint`/`primary`) with gradient backgrounds. Used by block rendering, detail pages, and listing pages.
- `page-section.html` — Lightweight fixed-style wrapper for blog/author listing pages. Gray gradient bg, centered title, `max-w-5xl` container.

**Layout partials:**
- `container.html` — Centered max-width wrapper with configurable size (`small`/`medium`/`large`) and width variants.
- `actions.html` — Renders action buttons/links with color-aware styling. Supports `button` (filled) and `link` (text) types with theme color variants.
- `icon.html` — Icon renderer with circle/float styles, theme-aware coloring, and size classes (xs–xl).

**Block partials** (`layouts/partials/blocks/`):
- Each block has a wrapper (e.g. `hero.html`) that calls `section.html`, plus a content partial (e.g. `hero-content.html`) with the actual markup.

**UI partials** (`layouts/partials/ui/`):
- `gradient-title.html` — Themed gradient heading (uses `printf` + `safeHTML` for dynamic tags).
- `badge.html` — Pill badge for tags/hobbies.
- `card.html` — Card container.
- `no-data.html` — Empty state fallback.

## Routes

| Route | Template | Description |
|-------|----------|-------------|
| `/` | `index.html` | Home page with block-based content |
| `/posts/` | `posts/list.html` | Post listing (single-column cards) |
| `/posts/:slug` | `posts/single.html` | Post detail (supports nested paths) |
| `/blog/` | `blogs/list.html` | Blog listing (2-column grid with hero images) |
| `/blog/:filename` | `blogs/single.html` | Blog detail |
| `/authors/` | `authors/list.html` | Author listing (2-column grid) |
| `/authors/:filename` | `authors/single.html` | Author detail |
| `/pages/:filename` | `pages/single.html` | Block-based pages |
| `/admin/` | `static/admin/index.html` | TinaCMS admin panel |

## Limitations

- No visual/inline editing; editing happens in the admin panel only.
- No framework-level image optimisation — uses standard `<img>` tags.
- Full page reloads on navigation (no SPA routing).
- Rich-text body renders via Goldmark; the shared `TinaMarkdown` custom templates are not supported.
- Global config hot-reload is unavailable — see below.

### Global config hot-reload

Hugo's dev server doesn't hot-reload data files from data mounts (e.g. `../../shared/content/global`). Content mounts (authors, tags, posts, etc.) hot-reload fine when edited. But `global/index.json` is read via `hugo.Data.global` (a data mount), and Hugo doesn't trigger a live reload when data mount files change. If you change `global/index.json` (theme, nav, footer, etc.) while the dev server is running, the changes won't appear until you restart `pnpm dev`.

## E2E Tests

Playwright suite in [e2e/](e2e/). App-specific structure:

- **Frontend** (6 files, fully parallel): `home`, `posts`, `blog`, `authors`, `navigation`, `edge-cases`
- **Admin CRUD** (4 files, serial per collection): `author`, `blog`, `post`, `page`
- **CI**: `.github/workflows/playwright-hugo-kitchen-sink.yml`

## Reference

- TinaCMS contextual editing docs: [tina.io/docs/contextual-editing/overview](https://tina.io/docs/contextual-editing/overview)

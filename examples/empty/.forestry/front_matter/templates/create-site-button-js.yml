---
label: create-site-button-js
hide_body: false
fields:
  - type: select
    config:
      source:
        type: simple
      options:
        - hugo
        - jekyll
        - vuepress
        - gatsby
        - other
      required: true
    label: Engine Name
    name: engine_name
  - type: text
    name: engine_version
    label: Engine Version
    config:
      required: false
  - type: text
    name: repo_name
    label: Repo Name
    config:
      required: true
  - type: text
    name: branch_name
    label: Branch Name
    config:
      required: true
  - type: text
    label: Fork Name
    name: fork_name
    config:
      required: true
    description: The name that the forked repo will default to

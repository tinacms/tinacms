{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/@tinacms/*",
    "examples/*",
    "packages/[^@]*"
  ],
  "scripts": {
    "g:echo": "echo $INIT_CWD",
    "g:tsc": "cd $INIT_CWD && tsc",
    "build": "yarn workspaces foreach --topological-dev --no-private run g:echo | cat > topologicalDeps.txt && yarn workspace @tinacms/tina-build build:all",
    "watch": "yarn workspace @tinacms/tina-build watch:all",
    "test": "yarn workspaces foreach --no-private -p run test",
    "g:nuke": "cd $INIT_CWD && rm -rf dist"
  },
  "dependencies": {
    "@changesets/cli": "^2.16.0",
    "@yarnpkg/pnpify": "^2.0.0-rc.20",
    "danger": "^10.6.4",
    "eslint": "^6.8.0",
    "prettier": "^2.3.2",
    "typescript": "^3.8.3",
    "workspace": "^0.0.1-preview.1"
  }
}

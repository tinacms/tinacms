{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --reject-cycles",
    "bootstrap": "lerna bootstrap --hoist --reject-cycles",
    "bs": "npm run bootstrap",
    "dev": "lerna run dev --reject-cycles",
    "release": "lerna publish --reject-cycles --conventional-commits --create-release github",
    "test": "lerna run test --reject-cycles",
    "watch": "lerna run watch --parallel --reject-cylcles"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "commander": "^2.20.0",
    "lerna": "^3.16.4",
    "rollup": "^1.21.4",
    "rollup-plugin-commonjs": "^10.0.1",
    "rollup-plugin-typescript2": "^0.22.0",
    "rollup-plugin-uglify": "^6.0.3",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "danger": "^9.2.0",
    "react-frame-component": "^4.1.1"
  }
}

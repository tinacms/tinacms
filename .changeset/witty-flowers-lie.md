---
"@tinacms/schema-tools": patch
"tinacms": patch
---

Added the ability to provide custom highlight colors for the MDX/RichText editor toolbar. This allows developers to override the default color palette by passing a highlightColors array to the field schema.

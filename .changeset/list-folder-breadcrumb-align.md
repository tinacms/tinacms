---
"tinacms": patch
---

fix(tinacms): align the admin collection-list folder breadcrumb with the editor breadcrumb — replace the bare home icon and the separate back button with a single labelled back-arrow crumb that returns to the collection one folder level up, and restyle the folder crumbs (gray text, orange hover, slash separators) to match
